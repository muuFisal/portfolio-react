# fisal-portfolio (React + TS + Tailwind + i18n + Dark/Light)

## Run
```bash
npm install
npm run dev
```

## Notes
- Change profile links in `src/data/meta.ts`
- Edit projects in `src/data/projects.ts`
- Edit translations in `src/i18n/locales/*/translation.json`
